[build]
  command = "npm run build"
    publish = "dist"

    [build.environment]
      NODE_VERSION = "18"

      # Redirects are managed in public/_redirects — do NOT add a catch-all here.
      # The _redirects file includes an explicit /wok-data.json rule before the
      # SPA fallback (/* /index.html 200), which is required for the WoK engine.
