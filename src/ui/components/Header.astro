---
import SearchBox from './SearchBox.astro';

export interface Props {
  title?: string;
}

const { title = '' } = Astro.props as Props;
---

<header class="border-b bg-white dark:bg-gray-900">
  <div class="max-w-7xl mx-auto px-4 py-3 flex items-center">
    <a href="/" class="flex items-center gap-3 no-underline">
      <span class="text-4xl md:text-5xl font-bold leading-none text-blue-600">⚓</span>
      <span class="text-xl md:text-2xl font-semibold text-gray-900 dark:text-gray-100" style="transform: scale(1.3);">Sailor Success</span>
    </a>
    <div class="ml-auto">
      <SearchBox />
    </div>
  </div>
</header>
